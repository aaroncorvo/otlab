# OTLab — ESPHome secrets. Lab convention (intentionally public);
# rotate for non-lab use. Same posture as the rest of the credentials
# table in notion-bundle/00-OTLab-Index.md.
#
# These are referenced from per-device YAMLs via the `!secret` syntax.

wifi_ssid: "MFCTP"
wifi_password: "P@ssw0rd!"
ap_password: "P@ssw0rd!"

# ESPHome native API key (used by Home Assistant / ESPHome Dashboard).
# Generated once with `python3 -c "import secrets; print(secrets.token_urlsafe(32))"`
api_key: "c3KGRgULut5Gfk0a2RV+hzU7qx6dTi55Bgq4nT2AEKg="

# OTA password — used to authenticate OTA flashes over the network.
ota_password: "P@ssw0rd!"
